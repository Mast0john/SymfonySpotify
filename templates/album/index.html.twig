{# <h1>Album</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for album in albums %}
            <tr>
                <td>{{ album.name }}</td>
                <td>
                    <a class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" href="{{ path('app_album_show', {'id': album.id}) }}">Voir</a>
                    {% if is_granted('ROLE_ARTIST') %}
                        <a class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" href="{{ path('app_album_edit', {'id': album.id}) }}">Modifier</a>
                    {% endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Playlist vide</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

 {% if is_granted('ROLE_ARTIST') %}
    <a class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" href="{{ path('app_album_new') }}">Créer un album</a>
 {% endif %} #}





 <div class="mt-10">
    <!-- song list header -->
    <div class="flex text-gray-600">
        <div class="p-2 w-8 flex-shrink-0"></div>
        <div class="p-2 w-8 flex-shrink-0"></div>
        <div class="p-2 w-full">Albums</div>
    </div>

    {% for album in albums %}
        <div class="flex border-b border-gray-800 hover:bg-green-700/25">
            <div class="p-3 w-full">{{ album.name }}</div>
            <div class="p-3 w-32 flex-shrink-0 text-right"><a href="{{ path('app_album_show', {'id': album.id}) }}">👀 voir</a></div>
            {% if is_granted('ROLE_ARTIST') %}
                <div class="p-3 w-32 flex-shrink-0 text-right"><a href="{{ path('app_album_edit', {'id': album.id}) }}">✏️ modifier</a></div>
            {% endif %}
        </div>
    {% else %}
        <div class="flex border-b border-gray-800 hover:bg-gray-800">
            <p>Aucun album</p>
        </div>
    {% endfor %}
</div>


{% if is_granted('ROLE_ARTIST') %}
    <button class="bg-teal-700/70 hover:bg-teal-700 text-white font-bold py-2 rounded shadow-lg hover:shadow-xl transition duration-200 p-2 m-2"><a href="{{ path('app_album_new') }}">Créer un album</a></button>
{% endif %}