{% extends 'base.html.twig' %}

{% block title %}Modifier une playlist{% endblock %}

{% block body %}
    <h1>Modifier une {{ playlist.name }}</h1>

    {{ include('playlist/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_homepage') }}">Accueil</a>

    {{ include('playlist/_delete_form.html.twig') }}
{% endblock %}
